# Introspection

This package contains code for analysing the Sumo-Tools codebase.

The purpose is to understand how the project works well enough to deploy it elsewhere, starting with no data.

The intended mechanism is `make`. The objective of this work is therefore to create a Makefile that can rebuild the project outputs from the necessary sources and intermediate steps.
