# Open Issues

1. At the moment, the initial docs for this project are in `src/docs/makefile`. The entire contents of that folder should be moved to `src/introspection/docs`, and a new `src/docs/makefile/README.md` should be created saying: `see introspection/README.md`.
