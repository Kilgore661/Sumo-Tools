# This has the original version of Ann

from enum import Enum, auto

class NewAnn(Enum):
    #! Val: E2.1.2.6
    TD = auto()  
    OB = auto() 
    HD = auto() 
    YO = auto() 
    EMPTY = auto()  # Empty annotation (ε)
